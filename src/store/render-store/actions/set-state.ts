import { usePayload } from '@/hooks'

/**
 * 设置组件状态（值/选项等）
 */
export function setState(options: any) {
  const { payloads } = options
  const payload = usePayload(payloads)

  // store

  // modelValue

  // property
}

